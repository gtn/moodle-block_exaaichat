<?xml version="1.0"?>
<ruleset name="CustomRuleset">
    <rule ref="Moodle"/>

    <!-- This doesn't work: Exclude vendor directory -->
    <exclude-pattern>vendor/*</exclude-pattern>

    <!-- Disable the specific sniff: this does not work! -->
    <exclude name="moodle.NamingConventions.ValidVariableName.VariableNameUnderscore"/>
    <exclude name="moodle.Files.LineLength.TooLong"/>
</ruleset>
